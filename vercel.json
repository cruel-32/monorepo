{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "scripts": {
    "build": "yarn set version latest && yarn install && yarn build"
  }
}
